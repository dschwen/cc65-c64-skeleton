# Script/conversation interpreter executed directly from bank 47 ROML.
# Mutable state is resident below $8000; code and constants are ROM-only.
MEMORY {
    ZP:      file = "", start = $0002, size = $001A;
    BANKED:  file = %O, start = $8800, size = $0B00;
}

SEGMENTS {
    ENTRY:    load = BANKED, type = ro;
    CODE:     load = BANKED, type = ro, optional = yes;
    RODATA:   load = BANKED, type = ro, optional = yes;
    DATA:     load = BANKED, type = ro, optional = yes;
    ZEROPAGE: load = ZP,     type = zp, optional = yes;
}
